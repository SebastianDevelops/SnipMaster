<Page
    x:Class="SnippetMasterWPF.Views.Pages.FileConversionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnippetMasterWPF.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DataContext="{d:DesignInstance local:FileConversionPage, IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <ScrollViewer Padding="16">
                <StackPanel>
                    <TextBlock Text="File Conversion" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <!-- Image Conversion -->
                    <Expander Header="Image Conversion" IsExpanded="True" Margin="0,0,0,16">
                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="Convert To:" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding ViewModel.ImageFormats}" 
                                     SelectedItem="{Binding ViewModel.SelectedImageFormat}"
                                     MaxDropDownHeight="150" Margin="0,0,0,12"
                                     ScrollViewer.CanContentScroll="True"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                            <ui:Button Content="Select Image Files" 
                                      Command="{Binding ViewModel.SelectImageFilesCommand}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Expander>

                    <!-- Document Conversion -->
                    <Expander Header="Document Conversion" Margin="0,0,0,16">
                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="Convert To:" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding ViewModel.DocumentFormats}" 
                                     SelectedItem="{Binding ViewModel.SelectedDocumentFormat}"
                                     MaxDropDownHeight="150" Margin="0,0,0,12"
                                     ScrollViewer.CanContentScroll="True"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                            <ui:Button Content="Select Document Files" 
                                      Command="{Binding ViewModel.SelectDocumentFilesCommand}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Expander>

                    <!-- Video/Audio Conversion -->
                    <Expander Header="Video/Audio Conversion" Margin="0,0,0,16">
                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="Convert To:" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding ViewModel.VideoAudioFormats}" 
                                     SelectedItem="{Binding ViewModel.SelectedVideoAudioFormat}"
                                     MaxDropDownHeight="150" Margin="0,0,0,12"
                                     ScrollViewer.CanContentScroll="True"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                            <ui:Button Content="Select Media Files" 
                                      Command="{Binding ViewModel.SelectMediaFilesCommand}"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="File Conversion Center" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Select files from the sidebar to convert between different formats" 
                          Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Tab View -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
                <TabControl.Style>
                    <Style TargetType="TabControl">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabControl">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" 
                                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                                                BorderThickness="1" CornerRadius="8,8,0,0" Padding="4,4,4,0">
                                            <TabPanel IsItemsHost="True" Background="Transparent"/>
                                        </Border>
                                        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                                                BorderThickness="1,0,1,1" CornerRadius="0,0,8,8">
                                            <ContentPresenter ContentSource="SelectedContent" Margin="0"/>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Style>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Name="Border" Background="{DynamicResource SubtleFillColorTransparentBrush}" 
                                            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                                            BorderThickness="1,1,1,0" CornerRadius="6,6,0,0" 
                                            Margin="2,0,0,0" Padding="16,8">
                                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                         TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
                                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                                            <Setter Property="Panel.ZIndex" Value="1"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <!-- Convert Tab -->
                <TabItem Header="Convert">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- File List -->
                        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                                CornerRadius="8">
                            <Grid>
                                <ListView ItemsSource="{Binding ViewModel.ConversionItems}" 
                                         Background="Transparent" BorderThickness="0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Row="0" Grid.Column="0">
                                                    <TextBlock Text="{Binding FileName}" FontWeight="Medium"/>
                                                    <TextBlock Text="{Binding FilePath}" 
                                                              Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                                              FontSize="12"/>
                                                </StackPanel>

                                                <!-- PDF to Image Controls Grid -->
                                                <Grid Grid.Row="1" Grid.Column="0" Margin="0,8,0,4">
                                                    <Grid.Style>
                                                        <Style TargetType="Grid">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsPdfToImage}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Grid.Style>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <TextBlock Grid.Column="0" Text="Page:" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                                                    
                                                    <ui:NumberBox Grid.Column="1" Value="{Binding SelectedPage}" Minimum="1" Maximum="{Binding TotalPages}" 
                                                                  Margin="0,0,8,0" FontSize="13" VerticalAlignment="Center"/>
                                                    
                                                    <TextBlock Grid.Column="2" Text="{Binding TotalPages, StringFormat='of {0}'}" VerticalAlignment="Center" 
                                                               Margin="0,0,16,0" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                    
                                                    <ui:TextBox Grid.Column="3" PlaceholderText="Password" Text="{Binding PdfPassword}" 
                                                                FontSize="13" VerticalAlignment="Center">
                                                        <ui:TextBox.Style>
                                                            <Style TargetType="ui:TextBox">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding RequiresPassword}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </ui:TextBox.Style>
                                                    </ui:TextBox>
                                                </Grid>

                                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Status}" 
                                                          VerticalAlignment="Center" Margin="8,0"/>

                                                <ProgressBar Grid.Row="0" Grid.Column="2" Value="{Binding Progress}" 
                                                           Width="100" Height="6" VerticalAlignment="Center" 
                                                           Margin="8,0">
                                                    <ProgressBar.Style>
                                                        <Style TargetType="ProgressBar">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsConverting}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ProgressBar.Style>
                                                </ProgressBar>

                                                <ui:Button Grid.Row="0" Grid.Column="3" Content="Remove" 
                                                          Command="{Binding DataContext.ViewModel.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                          CommandParameter="{Binding}" Margin="8,0,0,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <!-- Empty State -->
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ViewModel.HasItems}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <ui:SymbolIcon Symbol="DocumentArrowRight24" FontSize="48" 
                                                  Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,16"/>
                                    <TextBlock Text="No files selected for conversion" 
                                              FontSize="16" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="Use the sidebar to select files and choose output formats" 
                                              Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                              HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                            <ui:Button Content="Clear All" Command="{Binding ViewModel.ClearAllCommand}" 
                                      Margin="0,0,8,0" Appearance="Secondary"/>
                            <ui:Button Content="Convert All" Command="{Binding ViewModel.ConvertAllCommand}" 
                                      Appearance="Primary"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- History Tab -->
                <TabItem Header="History">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Search Box -->
                        <ui:TextBox Grid.Row="0" PlaceholderText="Search converted files..." 
                                   Text="{Binding ViewModel.HistorySearchText}" Margin="0,0,0,16" >
                            <ui:TextBox.Icon>
                                <ui:SymbolIcon Symbol="Search24"/>
                            </ui:TextBox.Icon>
                        </ui:TextBox>

                        <!-- History List -->
                        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" 
                                CornerRadius="8">
                            <Grid>
                                <ListView ItemsSource="{Binding ViewModel.FilteredHistoryItems}" 
                                         Background="Transparent" BorderThickness="0">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <ui:SymbolIcon Grid.Column="0" Symbol="{Binding FileIcon}" 
                                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Center"/>

                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"  Text="{Binding FileName}" FontWeight="Medium"/>
                                                    <TextBlock Text="{Binding ConvertedDate}" 
                                                              Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                                              FontSize="12"/>
                                                    <TextBlock Text="{Binding FilePath}" 
                                                              Foreground="{DynamicResource TextFillColorTertiaryBrush}" 
                                                              FontSize="11"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="2" Text="{Binding FileSize}" 
                                                          VerticalAlignment="Center" Margin="8,0" 
                                                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

                                                <ui:Button Grid.Column="3" Content="Open Folder" 
                                                          Command="{Binding DataContext.ViewModel.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                          CommandParameter="{Binding}" Margin="8,0,0,0" Appearance="Secondary"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <!-- Empty History State -->
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ViewModel.HasHistoryItems}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <ui:SymbolIcon Symbol="History24" FontSize="48" 
                                                  Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,16"/>
                                    <TextBlock Text="No conversion history" 
                                              FontSize="16" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                              HorizontalAlignment="Center"/>
                                    <TextBlock Text="Converted files will appear here" 
                                              Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                              HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Page>