<Page
    x:Class="SnippetMasterWPF.Views.Pages.GalleryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SnippetMasterWPF.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="GalleryPage"
    d:DataContext="{d:DesignInstance local:GalleryPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Generated Screenshots" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
            <Button Margin="20,0,0,0" Command="{Binding ViewModel.RefreshCommand}" Content="Refresh" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ViewModel.Screenshots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Padding="10" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                                CornerRadius="8" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Width="220">
                                <Image Source="{Binding Thumbnail}" Height="150" Stretch="Uniform" />
                                <TextBlock Text="{Binding FileName}" FontWeight="Bold" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{Binding CreatedDate, StringFormat='Created: {0:MM/dd/yyyy HH:mm}'}" 
                                          FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <Button Content="Open" Margin="0,0,5,0" 
                                           Command="{Binding DataContext.ViewModel.OpenFileCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding FilePath}" />
                                    <Button Content="Delete" 
                                           Command="{Binding DataContext.ViewModel.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                           CommandParameter="{Binding FilePath}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>